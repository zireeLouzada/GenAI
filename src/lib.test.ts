import {describe,expect,it} from 'vitest'; import {compactBRL,money,parseMoney,percentage} from './lib';
describe('formatadores financeiros',()=>{it('formata BRL em pt-BR',()=>expect(money(180397.19)).toContain('180.397,19'));it('formata BRL compacto para eixos',()=>{expect(compactBRL(0)).toBe('R$ 0');expect(compactBRL(370)).toBe('R$ 370');expect(compactBRL(1000)).toBe('R$ 1 mil');expect(compactBRL(2500)).toBe('R$ 2,5 mil');expect(compactBRL(100000)).toBe('R$ 100 mil')});it('interpreta entrada monetária brasileira',()=>expect(parseMoney('R$ 1.234,56')).toBe(1234.56));it('limita e remove zeros do percentual',()=>{expect(percentage(45)).toBe('45%');expect(percentage(120)).toBe('100%');expect(percentage(-2)).toBe('0%')})});
