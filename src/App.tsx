import {Building2,ChartNoAxesCombined,Menu,ReceiptText,X} from 'lucide-react'; import {useState} from 'react'; import {NavLink,Route,Routes} from 'react-router-dom'; import {Toaster} from 'sonner'; import {Dashboard} from './pages/Dashboard'; import {Expenses} from './pages/Expenses'; import {ApartmentPage} from './pages/Apartment'; import {useStore} from './store';
export default function App(){const [menu,setMenu]=useState(false),{demo}=useStore();const links=[['/',ChartNoAxesCombined,'Dashboard'],['/gastos',ReceiptText,'Gastos'],['/imovel',Building2,'Dados do imóvel']] as const;return <div className="app"><aside className={menu?'sidebar open':'sidebar'}><div className="brand"><span><Building2/></span><div><strong>Meu Apê</strong><small>Controle financeiro</small></div></div><button className="close-menu" onClick={()=>setMenu(false)}><X/></button><nav>{links.map(([to,Icon,label])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setMenu(false)}><Icon/>{label}</NavLink>)}</nav><div className="sidebar-foot"><span className="status-dot"/> {demo?'Modo demonstração':'Conectado ao Supabase'}</div></aside><main><button className="menu-button" onClick={()=>setMenu(true)}><Menu/></button>{demo&&<div className="demo-banner">Supabase não configurado — os dados estão sendo salvos apenas neste navegador.</div>}<div className="content"><Routes><Route path="/" element={<Dashboard/>}/><Route path="/gastos" element={<Expenses/>}/><Route path="/imovel" element={<ApartmentPage/>}/></Routes></div></main>{menu&&<div className="backdrop" onClick={()=>setMenu(false)}/>}<Toaster richColors position="top-right"/></div>}
